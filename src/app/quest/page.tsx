import { QuestWorkspace } from "@/components/quest/QuestWorkspace";

export const metadata = {
  title: "Web Quest | Ancient Human Relatives",
  description: "Complete the Ancient Human Relatives guided research web quest.",
};

export default function QuestPage() {
  return <QuestWorkspace />;
}
